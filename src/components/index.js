// Loading.js

import React from 'react';
import './Loading.css'; // Import your CSS for loading animation

const Loading = () => {
  return (
    <div className="loading">
      <div className="pulse"></div>
    </div>
  );
};

export default Loading;
